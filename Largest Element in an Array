
class Solution {
    public static int largest(int[] arr) {
        int res = Integer.MIN_VALUE;
        int m = arr.length;
        for(int i=0; i<m; i++){
            res = Math.max(res,arr[i]);
        }
        return res;
    }
}
